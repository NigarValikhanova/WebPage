@model Blog

<form asp-controller="Blog" asp-action="Edit" method="Post" enctype="multipart/form-data">

        <div class="mb-3">
        <label class="form-label">Blog Image</label>
        <img id="img-preview" src="~/images/blog/@Model.ImageUrl" width="250px" />
        <div>
            <input asp-for="File" type="file" accept="image/*" onchange="readURL(this)" />
        </div>
    </div>

    <div class="mb-3">
        <label class="form-label">Blog Name</label>
        <input asp-for="Name" type="text" class="form-control">
    </div>

    <div class="mb-3">
        <label class="form-label">Blog Context</label>
        <input asp-for="Context" type="text" class="form-control">
    </div>

    <div class="mb-3">
        <label class="form-label">Blog Username</label>
        <input asp-for="UserName" type="text" class="form-control">
    </div>

    <div class="mb-3">
        <label class="form-label">Blog Date</label>
        <input asp-for="ReleaseDate" type="datetime" class="form-control">
    </div>

    <button class="btn btn-success">Submit</button>

</form>

@section Script{
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script>
        let noimage =
            "https://ami-sni.com/wp-content/themes/consultix/images/no-image-found-360x250.png";

        function readURL(input) {
            console.log(input.files);
            if (input.files && input.files[0]) {
                var reader = new FileReader();
                reader.onload = function (e) {
                    $("#img-preview").attr("src", e.target.result);
                };

                reader.readAsDataURL(input.files[0]);
            } else {
                $("#img-preview").attr("src", noimage);
            }
        }

    </script>
        }